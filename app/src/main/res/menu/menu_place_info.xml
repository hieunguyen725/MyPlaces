<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="controller.PlaceInfoActivity">
    <item android:id="@+id/action_settings" android:title="@string/action_settings"
        android:orderInCategory="100" app:showAsAction="never" />


    <item
        android:id="@+id/related_search"
        android:title="@string/related_search"
        app:showAsAction="always"/>

    <item
        android:id="@+id/nearby_search"
        android:title="@string/nearby_search"
        app:showAsAction="always"/>

    <item
        android:id="@+id/my_places"
        android:title="@string/my_places"
        app:showAsAction="always"/>

</menu>
